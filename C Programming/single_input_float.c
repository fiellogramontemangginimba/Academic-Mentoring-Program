#include <stdio.h>

int main() {
    // declare variable
    float num;

    // get user input
    scanf("%f", &num);

    // display user input
    printf("value entered: %d", num);

    // exit program
    return 0;
}
