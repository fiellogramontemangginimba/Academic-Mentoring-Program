#include <stdio.h>

int main() {
    for(int num = 5; num > 0; num--) {
        // run everything inside {}
        printf("num is equal to %d\n", num);
    }

    printf("loop finished");

    // exit program
    return 0;
}
