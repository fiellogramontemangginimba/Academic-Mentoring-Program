#include <stdio.h>

int main() {
    // declare variable
    float num;

    // get user input
    scanf("%f", &num);

    // display user input
    // printf("value entered: %d", num);
    // use %f to print a float
    printf("value entered: %f", num);

    // exit program
    return 0;
}
