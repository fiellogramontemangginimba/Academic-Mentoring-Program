#include <stdio.h>

int main() {
    // declare variable
    char character;

    // get character
    printf("enter character: ");
    scanf("%c", &character);
    // display character
    printf("character entered: %c", character);

    // exit program
    return 0;
}
