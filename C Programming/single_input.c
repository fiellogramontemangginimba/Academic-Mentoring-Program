#include <stdio.h>

int main() {
    // declare variable
    int num;

    // get user input
    scanf("%d", &num);

    // display user input
    printf("value entered: %d", num);

    // exit program
    return 0;
}
